application.yml (YAML) — файлов формат за конфигурация с подредена структура, използващ отстъпи.

YAML използва отстъпи (индентация) за структура.

Чете се като дървовидна структура — удобно за по-сложни и йерархични настройки